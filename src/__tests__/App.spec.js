import React from 'react';
import { render, waitFor } from '@testing-library/react';
import { App } from '../App'

describe('<App />', () => {
    it('renders the app with two pierogis already in the basket', () => {
        
    });
});
